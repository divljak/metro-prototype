// Prototype metadata. The design-system scan only looks at prototypes with
// status: "approved". Flip this when your prototype is published.
export const meta = {
  name: "Dashboard (provisional)",
  status: "approved" as "draft" | "approved" | "archived",
  owner: "@design-team",
}
